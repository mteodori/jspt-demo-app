/*global define*/

define([
    'jquery',
    'backbone'
], function ($, Backbone) {
    'use strict';

    var TweetRouter = Backbone.Router.extend({
        routes: {
        }

    });

    return TweetRouter;
});